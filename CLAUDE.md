# Discord Voice TTS Bot

特定の Discord サーバーに常駐し、Voice チャンネルのテキストを読み上げる Bot。

## 開発ルール

- TDD: 必ずテストコードを先に書いてから実装コードを書く
- テストは本質的なテストと境界値テストを含めること（形だけのテストは不可）
- TypeScript の実行（npm install、テスト、ビルド等）はすべて podman コンテナで行う
  - `npm install` は python3, make, g++ が必要（@discordjs/opus のネイティブビルド用）
  - 実行時は ffmpeg が必要（Dockerfile が取得するので追加の apt インストールは不要。画像処理は npm の sharp を使うため imagemagick は不要）
- Claude Code が .env ファイルを読み込むことは禁止（.env.example は可。Bot ランタイムの dotenv 使用は問題ない）
- コメントやテストの human-readable なテキストは英語で考えて日本語で記載する
- コーディングスタイル: ESLint ネイティブ Flat Config（旧 neostandard 相当: セミコロンあり、シングルクォート、インデント2スペース）。コードを書いたら必ず `npm run lint` でチェックし、違反があれば修正すること
- if 文の本体は必ず中括弧ブロックで囲む（ガード節のワンライナー禁止）
- 機能の追加・変更・削除を行った場合は CLAUDE.md の該当箇所も更新すること
- YAGNI: 現在必要な機能だけを実装する。将来の要件を想定した設定項目・拡張ポイント・汎用化は行わない
- DRY: 同じロジックが3箇所以上で繰り返される場合は共通化を検討する。ただし2箇所程度の類似コードや、共通化により可読性が下がる場合は重複を許容する

## 技術スタック

- 言語・ランタイム: TypeScript / npm / podman + node:26-slim（マルチステージ Dockerfile）
- テスト: Jest
- リンター: ESLint 10（ネイティブ Flat Config: `eslint.config.js`、@stylistic / typescript-eslint / eslint-plugin-n / eslint-plugin-promise を直接構成。旧 neostandard 相当のルール）
- CI: GitHub Actions（`.github/workflows/ci.yml`）— push / PR 時に lint・test・build を並行実行
- 主要ライブラリ: discord.js, @discordjs/voice, @discordjs/opus, openai（TTS・Chat API 用）, sharp（画像リサイズ・JPEG 変換）, yaml
- ffmpeg: Dockerfile の専用ステージで [BtbN/FFmpeg-Builds](https://github.com/BtbN/FFmpeg-Builds) の `ffmpeg-master-latest-linux64-lgpl.tar.xz` を取得し、`ffmpeg` バイナリのみを実行イメージへコピーする（apt の ffmpeg は使わない）
  - 理由: 常に最新の ffmpeg を使うため。加えて Debian の `apt-get install ffmpeg` は 359 パッケージ・629MiB を追加するが、外部ライブラリを静的リンクしたこのビルドは単一バイナリ 112MiB で済み、イメージが 1.06GB から 523MB に縮む
  - 前提: x86_64 かつ glibc 2.28 以上（バイナリの動的依存は glibc 系のみ）。arm64 で動かす場合は URL を `linuxarm64-lgpl` に差し替える必要がある
  - LGPL 版を選ぶ（GPL 版ではない）: 使用する `atempo` フィルタと opus エンコーダはどちらも LGPL 版に含まれており、GPL コードを含めない方がイメージ再配布時の制約が軽い
  - `latest` タグは master のローリングビルドなので、ビルドのたびに別のコミットが入る。ビルドを固定したい場合は `ffmpeg-nX.Y-latest-linux64-lgpl-X.Y.tar.xz` 形式のリリースビルドに切り替える
- 依存の overrides（`package.json` の `overrides`）
  - `tar: ^7.5.22` — `@discordjs/opus` → `@discordjs/node-pre-gyp` が要求する `tar ^6.1.11` は修正打ち切りで、npm audit が critical/high を「No fix available」として報告するため強制昇格。node-pre-gyp が使うのは `tar.extract({cwd, strip, onentry})` のみで、tar 7 の破壊的変更（Node 18+、ESM 化、`Parse`→`Parser` 等のクラス名変更、`chmod` デフォルト反転）はいずれも該当しない。node-pre-gyp が tar 7 に対応したら削除する

## ツール

- `./build.sh` — Docker イメージのビルド
- `./start.sh` — ビルド後に Bot を起動（`--skip-build` でビルドスキップ可）
- `./invite.sh <クライアントID>` — Bot 招待用 OAuth2 URL を生成

## 環境変数（.env）

| 変数名 | 用途 |
|---|---|
| `DISCORD_TOKEN` | Discord Bot トークン |
| `TTS_BASE_URL` | OpenAI 互換 TTS API のベース URL（`api.ai.sakura.ad.jp` を含む場合はさくらのAI Engine として検出） |
| `TTS_MODEL` | TTS モデル名 |
| `TTS_API_KEY` | TTS API キー |
| `TTS_VOICE` | デフォルトボイス |
| `CHAT_BASE_URL` | OpenAI 互換 Chat API のベース URL |
| `CHAT_MODEL` | Chat モデル名 |
| `CHAT_API_KEY` | Chat API キー |
| `CHAT_MULTI_MODAL` | マルチモーダル対応（`true` / `false`） |
| `TTS_SPEED` | 標準読み上げの再生速度（任意、デフォルト `1.5`、正の数） |
| `TTS_SUMMARY_SPEED` | URL要約・画像概要の再生速度（任意、デフォルト `1.75`、正の数） |

## 機能

### ボイスチャンネルへの参加・退出

- 起動時に既にユーザーがいるボイスチャンネルがあれば自動参加（同一ギルドで1チャンネルのみ）
- ユーザーが参加すると Bot も参加（同一ギルドで既に接続中なら参加しない、再登録時は旧接続を破棄）
- Bot 以外の全員が退出したら Bot も退出
- チャンネル移動時は旧チャンネルの退出処理を先に行い、その後に新チャンネルへの参加判定
- ミュート切替等チャンネル移動を伴わない状態変化は無視

### テキスト読み上げ

- Voice チャンネルの内蔵テキストチャット（GuildVoice type=2）への投稿を音声合成して再生
- すべての読み上げを ffmpeg atempo フィルタで再生（ピッチ変化なし、速度は `TTS_SPEED`/`TTS_SUMMARY_SPEED` で設定可能、デフォルトは標準1.5倍・URL要約/画像概要1.75倍）
- 同一ギルドはキューで順次処理、異なるギルドは並行処理（上限超過で破棄）
- キューは「準備（合成）」と「消費（再生）」を分離し、再生中に次の1件の合成を先読みする（発話間に合成待ちの無音が入らない。同時に合成するのは最大2件）
- 先読み済みのまま破棄された音声（`/queue-clear`・上限超過）はストリームを destroy して ffmpeg プロセスごと停止する
- TTS API の呼び出し（合成〜レスポンス読み取り）は最大3回リトライ。3回とも失敗した発言のみ破棄する（入力テキストが空の場合はリトライせず即エラー）
- 状態変化通知（参加・退出・配信開始/終了・カメラON/OFF）を読み上げる
  - Bot が現在接続中のチャンネルで発生した変化のみ通知（別チャンネルの参加・退出・配信・カメラは無視）
  - zundamon モデル時は語尾が変わる（例:「参加しました」→「参加したのだ」）
  - system 話者設定を使用
  - Bot も退出する場合は退出メッセージを読み上げない

### 音声キャッシュ（cache/）

- 状態変化通知（参加・退出・配信開始/終了・カメラON/OFF）は同一内容が繰り返し合成されるため、速度変換済みの音声（opus）を `cache/` にファイルとして保存し、2回目以降は TTS API を呼ばずに再生する
- キャッシュキーは テキスト・model・voice・速度 の sha256、ファイル名は `{ハッシュ}.opus`（速度も含むため `TTS_SPEED` を変えると再合成される）
- 通常のメッセージ読み上げ・URL要約・画像概要はキャッシュしない（同一テキストが繰り返されないため）
- 一時ファイルに書き出してから rename で差し替える（書き込み途中のファイルを読ませないため）。保存に失敗しても警告のみで再生は継続する
- エントリの自動削除は行わない。増えすぎた場合は `cache/` ごと削除すれば再生成される
- `start.sh` がホストの `cache/` を `/app/cache` にマウントするため、コンテナを作り直しても保持される

### 読み上げフォーマット

- 「{ユーザー名}、{本文}」形式（同一人物が10秒以内に連続発言した場合はユーザー名を省略）
- ユーザー名: ニックネーム優先 → 表示名にフォールバック（両方空なら名前なし）
- ユーザーメンション（`<@id>`）・ロールメンション（`<@&id>`）を `@名前` に解決してから本文処理（解決できないメンションは従来通り削除）
- 本文の事前処理: コードブロックを「コード省略」に置換 → インラインコードのバッククォート除去 → 引用を「引用省略」に置換 → カスタム絵文字削除 → Unicode 絵文字削除 → 未解決メンション削除 → URL を「URL」に置換 → 空白正規化（改行含む）
- ユーザー名にも同じサニタイズを適用
- 150文字超は切り取り「以下略」を付加
- 本文が空で添付ありの場合は枚数・本数を読み上げ（画像N枚・動画N本）

### 画像概要（マルチモーダル）

- `CHAT_MULTI_MODAL=true` かつ画像1枚のみ（テキスト・動画なし）・50MiB以下の場合に発動
- sharp で 1000x1000 以下にリサイズ・JPEG 変換 → Chat API で概要取得
- 「画像」を即座に読み上げ → 画像変換と並行して「概要：画像解析中...」をプレースホルダーとしてリプライ投稿 → 概要を非同期取得
- 成功時: 「概要：{テキスト}」を追加読み上げ＋プレースホルダーを概要に編集
- 概要が空の場合: プレースホルダーを削除、追加読み上げなし
- エラー時: プレースホルダーを「解析エラー」に編集、追加読み上げなし
- プレースホルダー投稿失敗時は概要取得後に新規リプライで投稿
- プレースホルダーの編集・削除・フォールバックリプライは最大3回リトライ
- 概要の読み上げ速度は `TTS_SUMMARY_SPEED`（標準読み上げの `TTS_SPEED` とは別）

### URL要約

- URLのみのメッセージを受信した場合、URLにアクセスしてページ内容を取得
- X/Twitter URL（x.com / twitter.com）は fxtwitter.com 経由でリクエスト（JS無効環境対応）
- テキスト系 content-type（text/*）の場合:
  - HTML の場合は script/style タグを除去後、全タグを除去してテキストを抽出
  - Chat API でページ内容を50文字程度に要約
  - 「要約：解析中...」プレースホルダーをリプライ → 完了後「要約：{要約}」に編集
- 対応画像形式（jpeg/png/gif/webp/bmp/tiff）かつマルチモーダル有効・50MiB以下の場合:
  - 画像概要と同じ解析フロー（processImage → describeImage）で概要を取得
  - プレースホルダーを「概要：{概要}」に編集
- ダウンロード失敗・未対応形式・要約/概要失敗・結果空の場合はプレースホルダーを削除
- プレースホルダー投稿失敗時は完了後に新規リプライで投稿
- プレースホルダーの編集・削除・フォールバックリプライは最大3回リトライ
- 要約・概要の読み上げ速度は `TTS_SUMMARY_SPEED` で設定（デフォルト1.75倍、標準読み上げの `TTS_SPEED` とは別）

### 要約・概要リプライの削除

- 元メッセージが削除されると、それに紐付く画像概要・URL要約のリプライも自動削除
- ギルドごとに直近200件の要約リプライを追跡（それより古いものは削除対象外）
- 解析中に元メッセージが削除された場合はプレースホルダーが削除され、その後の編集は失敗するため書き込みは残らない
- 概要が空・エラー等でプレースホルダーを削除した場合は追跡対象から外す
- リプライ削除は最大3回リトライ
- 読み上げキューに入った要約の再生は取り消さない（削除対象は書き込みのみ）

### スラッシュコマンド

- `/dictionary from to` — 読み上げ辞書にエントリを追加・更新（`to` が空または未指定の場合は削除）
- `/voice character style` — 読み上げボイスのカスタマイズ（さくらのAI Engine利用時のみ）
- `/voice-reset` — 読み上げボイスのカスタマイズをリセット（さくらのAI Engine利用時のみ）
- `/skip` — 現在の読み上げをスキップ（成功時は無応答）
- `/queue-size` — 読み上げキューの待機数を表示（全員に見えるリプライ）
- `/queue-clear` — 読み上げキューをクリア（全員に見えるリプライで消した件数を報告）
- `/dictionary` `/skip` `/queue-size` `/queue-clear` は常に登録、`/voice` `/voice-reset` はさくらのAI Engine利用時のみ登録
- `/skip` `/queue-size` `/queue-clear` はボイスチャンネルのチャットでのみ使用可（それ以外ではエフェメラルエラー）
- すべてギルド単位で登録（グローバル登録ではない）

### シャットダウン

- SIGINT/SIGTERM でボイス切断パケット送信後にクライアントを破棄（二重実行防止あり）

## 設定ファイル（config/）

すべて `config/` ディレクトリに配置（コンテナ起動時にディレクトリごとマウント）。各ファイルは `.gitignore` 対象、`.example` をテンプレートとして提供。

### channels.yml — チャンネルフィルタ

- ギルドIDをキーに、参加可能なチャンネルIDの配列を指定
- 未記載ギルド・`"*"` 指定・ファイルなしは全チャンネル許可

### dictionary.yml — グローバル読み上げ辞書

- キー → 値のマッピングで読み上げテキストを置換（ユーザー名・本文に適用、定型文には非適用）
- 全ルールを同時に適用（最長一致、一度変換された部分は再変換されない。キーの先頭・末尾が単語文字 `\w`（英数字・アンダースコア）の場合はその側に単語境界 `\b` を適用、それ以外は部分マッチ）
- ホットリロード対応（不正な内容の場合は前のルールを維持）
- 管理者が手動で編集する共通辞書

### dictionary-guild.yml — ギルド別読み上げ辞書

- ギルドIDをキーに、キー → 値のマッピングで読み上げテキストを置換
- `/dictionary` スラッシュコマンドで追加・更新・削除が可能（ギルド単位で保存）
- 同じキーワードはギルド辞書がグローバル辞書より優先
- ホットリロード対応（不正な内容の場合は前のルールを維持）

### speakers.yml — 話者設定

- ギルドIDをキーに model/voice のデフォルトと users 単位のオーバーライドを指定
- 特別なユーザーID `system` で状態変化通知の話者を設定
- 解決順序: ユーザー → ギルド → 環境変数
- ホットリロード対応（不正な内容の場合は前の設定を維持）
- `/voice` `/voice-reset` スラッシュコマンドで編集・リセットが可能（さくらのAI Engine利用時のみ）

### voice-members.log.yml — ボイスメンバーログ

- ボイスチャンネル参加ユーザーのID・表示名をギルド別に自動記録（speakers.yml 作成時のID参照用）
